// Freeze a single AI review in its own format. Never writes human reviewer kits.
import { readFile, writeFile } from 'node:fs/promises'
import { createHash } from 'node:crypto'
import { validateAiReferenceFiles } from './validate-ai-reference.mjs'
const directory = 'reports/ai-reference-2026-09-05'
const labelsPath = `${directory}/labels.json`
const selectionPath = '../NiyamLens_Field_Review_Kit_2026-09-05-v2/selection-manifest.json'
const validation = await validateAiReferenceFiles({ labelsPath, selectionPath, photoRoot: 'C:/Users/duvvu/Desktop/SIH/NiyamLens-Field-Pilot-2026-09-04-02' })
const hash = bytes => createHash('sha256').update(bytes).digest('hex')
const bytes = await readFile(labelsPath)
const labels = JSON.parse(bytes)
const counts = { readable: 0, 'not-visible': 0, illegible: 0 }
const perField = Object.fromEntries(['mrp', 'netQuantity', 'date'].map(field => [field, { ...counts }]))
for (const row of labels.rows) for (const [field, value] of Object.entries(row.fields)) { counts[value.status]++; perField[field][value.status]++ }
const freeze = { kind: 'frozen-single-AI-visual-reference-NOT-HUMAN-GROUND-TRUTH', finalizedAt: new Date().toISOString(), labelsSha256: hash(bytes), validation, counts, perField, supportingArtifacts: {}, ocrRunOnThisReference: false, tuningPermitted: false, limitations: ['One AI visual review, not two independent human reviews.', 'No OCR output, catalogue value or expected result was used to fill these labels.', 'Five readable fields across 24 images is insufficient to establish general recognition accuracy.', 'Not-visible refers only to this image, not the full package; it is not a legal absence finding.', 'No physical-package timing or qualified legal review was performed.'] }
for (const path of ['source-quality.json', 'viewing-derivatives.json']) freeze.supportingArtifacts[path] = hash(await readFile(`${directory}/${path}`))
await writeFile(`${directory}/freeze.json`, JSON.stringify(freeze, null, 2) + '\n', { flag: 'wx' })
const render = field => field.status === 'readable' ? field.value.replaceAll('|', '\\|') : field.status === 'illegible' ? 'Unreadable' : 'Not in view'
const table = labels.rows.map(row => `| ${row.itemId} | ${render(row.fields.mrp)} | ${render(row.fields.netQuantity)} | ${render(row.fields.date)} |`).join('\n')
const markdown = `# NiyamLens: 24-photo AI reference review\n\nSingle AI visual transcription, finalized ${freeze.finalizedAt}. This is **not independent human ground truth** and must not be submitted as Arif's or Tharun's work. Their review kits remain unchanged.\n\nAll 24 original JPEGs match the canonical selection hashes and decode successfully. Each target has visible evidence or an explicit reason for withholding a value in [labels.json](labels.json). The frozen label digest is \`${freeze.labelsSha256}\`.\n\n## Result\n\nAcross 72 target slots: **${counts.readable} readable, ${counts['not-visible']} not in view, ${counts.illegible} unreadable**. This is a description of the reference images, not OCR accuracy.\n\n| Photo | MRP | Package quantity | Packed/manufactured date |\n|---|---|---|---|\n${table}\n\n## Important qualifications\n\n- PUBLIC-015 uses informal package-mass wording, “I'm 40g of crunchiness.” Its transcription does not establish legal sufficiency.\n- PUBLIC-026 has a visible barcode/manifest product-key mismatch; both the original item ID and bytes were preserved. See its notes.\n- PUBLIC-016, 018 and 021 initially displayed JPEG rendering artifacts in the viewing tool. Strict decoding plus separately recorded PNG viewing derivatives exposed the complete frames. The originals were not repaired or modified.\n- Serving sizes, nutrient amounts, catalogue pack sizes, background handwriting, camera timestamps and relative shelf life were not substituted for target declarations.\n- “Not in view” does not mean the declaration is absent from the package. No legal verdict follows from these labels.\n\n## How the team may use this\n\nUse this as an AI-provisional reference for debugging an evaluation process and checking safe abstention. No OCR was run against these 24 during this review. Keep them out of development tuning; any later evaluation must identify these as AI-exposed and AI-labelled, not an independent human blind benchmark. Freeze and validate a future run against [freeze.json](freeze.json), retain failures, and separate readable-field recovery from false values on non-readable fields.\n\nThe set has only one readable MRP, one readable packing date and three readable quantity fields. A claim about real-world OCR needs a new, diverse declaration-focused set independently labelled by people. This review cannot replace physical-package workflow timing or a qualified Legal Metrology review.\n\n## Verify integrity\n\nFrom the niyamlens repository, run:\n\n\`\`\`powershell\nnode tools/validate-ai-reference.mjs --labels reports/ai-reference-2026-09-05/labels.json --selection ../NiyamLens_Field_Review_Kit_2026-09-05-v2/selection-manifest.json --photo-root C:/Users/duvvu/Desktop/SIH/NiyamLens-Field-Pilot-2026-09-04-02\n\`\`\`\n\nValidation checks AI-only provenance, the exact set of 24 IDs, all source hashes, null withheld values and the canonical selection. It does not certify the correctness of an AI's reading. Source attribution and rights remain in the original field-review kit and acquisition manifest.\n`
const frozenMarkdown = markdown.replace('--photo-root C:/Users/duvvu/Desktop/SIH/NiyamLens-Field-Pilot-2026-09-04-02\n', `--photo-root C:/Users/duvvu/Desktop/SIH/NiyamLens-Field-Pilot-2026-09-04-02 --freeze ${directory}/freeze.json\n`)
  .replace('null withheld values and the canonical selection.', 'null withheld values, the canonical selection and the frozen label digest. The digest detects drift; it is not a signature.')
await writeFile(`${directory}/README.md`, frozenMarkdown, { flag: 'wx' })
console.log(JSON.stringify({ labelsPath, labelsSha256: freeze.labelsSha256, counts, perField, validationPassed: validation.valid }))
