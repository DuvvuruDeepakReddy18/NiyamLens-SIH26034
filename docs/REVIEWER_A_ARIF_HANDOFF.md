# Private review handoff — Arif (Reviewer A)

You have been assigned **Reviewer A** for the NiyamLens 24-photo extraction pilot. Assignment is not proof of identity or independence; the statements in the form remain yours to make truthfully.

## What the coordinator gives you

A fresh private copy of the complete `NiyamLens_Field_Review_Kit_2026-09-05-v2` directory. Its dataset is `niyamlens-reserved-24-2026-09-05` and its selection hash begins `c109d82cbad0d78f`.

Do not use the older unversioned kit. Do not upload or publish the directory: it embeds licensed package photographs.

## Review rules

1. Before starting, do not open the exploratory OCR output, catalogue/product answers, a NiyamLens scan of these images, or Tharun's answers.
2. Open `review.html` locally in current Chrome or Edge. The page is self-contained and blocks network requests.
3. Choose **Independent reviewer A**. Create a short reviewer code using letters, digits, dots, underscores, or hyphens. Do not use a password or email address. Tell the coordinator privately which code is yours.
4. Check each attestation only if it is true. If you have already seen OCR or another review, stop and tell the coordinator; do not make a false attestation.
5. Review all 24 photographs. For each MRP, net quantity, and pack/manufacture date:
   - choose **Readable** only when the complete value is visibly legible, then enter exactly what the photograph supports;
   - use a normalized MRP such as `22.00`, a quantity such as `500 ml`, and the exact printed pack/manufacture date such as `19/10/25`;
   - do not infer missing digits, repair likely OCR mistakes, substitute an expiry/use-by date, or look up the product;
   - choose `not visible`, `illegible`, or `ambiguous` when that is what this single photograph supports.
6. Select the matching photo-readability status and add concise notes for glare, crop, blur, conflicts, or uncertainty.
7. When the progress reads `24 / 24 photos complete`, choose **Export completed review JSON**. Keep the original downloaded JSON unchanged.
8. Return only that JSON through the private channel agreed with the coordinator. Do not send it to Tharun and do not compare answers until the coordinator has retained both original file hashes.

The export is a self-attested observation, not a legal verdict, verified identity, or accuracy result.
