# Server-side only. Never expose this value through a VITE_ variable.
GOOGLE_CLOUD_VISION_API_KEY=

# Browser-safe project URL and publishable/anon key (never service_role).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server only. Match the browser project; do not prefix these with VITE_.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
